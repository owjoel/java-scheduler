spring:
  application:
    name: omnicron

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/omnicron}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate